window._env_ = {
    // This file is a fallback for local development.
    // In production (Docker), this file is overwritten dynamically by env.sh
    // using the environment variables passed to the container.
    // Example: API_URL: "http://localhost:8080/v1"
};
